#### 1. Where do you store your Docker images? Do you use Docker Hub?

Docker images are stored in:

Docker Hub (public/private)

Amazon ECR (preferred for AWS integration)

GitHub Container Registry (optional)
